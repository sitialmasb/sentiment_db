# 💬 Sentiment Emoji: Simple Sentiment Analysis

This Python project utilizes pre-trained models from the **Hugging Face Transformers** library to perform **Sentiment Analysis** on input text, displaying the results using corresponding **emojis** (e.g., 😊 for Positive, 🙁 for Negative).

---

## ⚙️ Prerequisites

You must have **Python 3.6 or newer** installed on your system.

---

## 🛠️ Installation and Setup

To run this script, you need to install the following Python libraries: `transformers`, a machine learning backend (`torch` or `tensorflow`), and `emoji`.

### 1. Install Core Libraries

Use `pip` to install the main NLP and emoji handling libraries:

```bash
pip install transformers emoji
